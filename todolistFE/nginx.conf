events {
  worker_connections 1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  sendfile on;
  keepalive_timeout 65;

  server {
    listen 80;
    server_name _;

    root  /usr/share/nginx/html;
    index index.html;

    # SPA-routing: servera index.html för paths som inte är riktiga filer
    location / {
      try_files $uri $uri/ /index.html;
    }

    # Proxy ALL /api/* calls to backend container (Compose service name)
    location /api/ {
      proxy_pass         http://backend-api:8080/;
      proxy_set_header   Host $host;
      proxy_set_header   X-Real-IP $remote_addr;
      proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
      proxy_http_version 1.1;
      proxy_buffering    off;  # bra för streaming/JSON i dev
    }

    # Bas-hårdare headers (valfritt)
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
  }
}